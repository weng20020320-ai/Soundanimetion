# Screenshots for clearmika.com — 已废弃

> **状态：DEPRECATED（2026-05-12 起）**
>
> 主页 Wavelet 卡片**不再使用静态截图**作为封面。改用 `docs/handoff/wavelet-cover.tsx`
> 中的同心圆脉冲动画组件作为卡片封面，详情弹窗里只展示文字 + Try online 大按钮。
>
> 理由：
> - Wavelet 的核心价值是"会动"，静态图无法表现节拍同步 / 波形流动
> - 动画组件 ~2.5 KB，比 PNG（after 优化 ~30 KB）首屏更快
> - 维护成本：UI 改了不需要重截图
>
> 这个目录保留为空，将来如果需要分发**离线宣传素材**（比如发到其他网站、社交媒体）
> 才会重新放截图。主页接入完全不依赖本目录。

## 如果将来仍需要截图

参考下面的旧规范（保留作为历史 / 备用文档）。

### 命名规范

| 文件名 | 用途 | 尺寸 | 备注 |
|---|---|---|---|
| `cover.png` | 主图 | **1920×1080**（16:9） | 备用 |
| `detail-01.png` ~ `detail-03.png` | 详情附图 | 1920×1080 或 1080×1920 | 备用 |

### 截图建议（旧）

参考 clearmika.com 主页气质（深空海军蓝、安静、克制）：

- 程序窗口处于**深色背景**下（默认 `bgColor: '#0b0d12'` 即可）
- 加载一段能让画面"动"起来的音频，调出**最克制的渐变**（推荐 `midnight-violet`、`twilight-glacier`、`carbon-fade` 这类雾系/冷色）
- **不要**用过分鲜艳的霓虹色或彩虹色，那会和主页气质冲突
- 推荐预设：`wave-line`（流光波形线，安静）、`area-spectrum`（填充频谱，干净）、`shader-flow`（频谱流体，抽象）
- 截图时把 Tweakpane 参数面板**隐藏或最小化**，让画面成为主体
- 在 1920×1080 全屏或近似全屏窗口下截图（不要带浏览器 / Windows 标题栏）
